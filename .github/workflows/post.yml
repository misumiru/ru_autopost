name: Post to X at 08:00/19:00 JST

on:
  schedule:
    - cron: '0 23 * * *'   # 08:00 JST（= 前日の23:00 UTC）
    - cron: '0 10 * * *'   # 19:00 JST（= 10:00 UTC）
  workflow_dispatch: {}     # 手動実行ボタン

permissions:
  contents: write           # 履歴をコミットするため

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install deps
        run: |
          python -m pip install --upgrade pip tweepy

      - name: Post once (JST)
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          # X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}   # 任意
        run: |
          python post_jst.py

      - name: Commit history
        run: |
          if [ -f ".state/history.json" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add .state/history.json
            git commit -m "chore: log post $(date -u +'%Y-%m-%dT%H:%M:%SZ')" || echo "no changes"
            git push
          fi
